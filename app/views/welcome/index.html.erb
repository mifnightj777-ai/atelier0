<div class="min-h-screen bg-white flex flex-col relative overflow-hidden font-['Inter']">

  <div class="absolute top-[-10%] right-[-5%] w-[600px] h-[600px] bg-indigo-50 rounded-full blur-[100px] pointer-events-none opacity-60 animate-float-slow"></div>
  <div class="absolute bottom-[-10%] left-[-10%] w-[500px] h-[500px] bg-slate-50 rounded-full blur-[80px] pointer-events-none animate-float-reverse-slow"></div>

  <nav class="relative z-50 w-full px-8 py-6 flex justify-between items-center animate-fade-in">
    <div class="flex items-center gap-2">
      <span class="w-3 h-3 rounded-full bg-indigo-600"></span>
      <span class="font-['Poppins'] font-bold text-xl tracking-tight text-slate-900">Atelier0.</span>
    </div>
    
    <div class="flex items-center gap-6">
      <%= link_to "GUIDE", guide_path, class: "text-xs font-bold text-slate-400 hover:text-indigo-600 transition-colors uppercase tracking-widest cursor-pointer relative z-50" %>
      
      <% if user_signed_in? %>
        <%= link_to "Back to Studio", fragments_path, class: "text-xs font-bold text-slate-900 hover:text-indigo-600 transition-colors relative z-50" %>
      <% end %>
    </div>
  </nav>

  <div class="flex-1 flex flex-col items-center justify-center px-6 relative z-10 text-center -mt-32 pb-20">
    
    <div class="mb-6 inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-white border border-slate-100 shadow-xl shadow-indigo-100/50 mb-8 animate-fade-in-up">
      <span class="font-['Poppins'] font-bold text-3xl text-indigo-600">A.</span>
    </div>

    <h1 class="font-['Poppins'] font-bold text-5xl md:text-7xl text-slate-900 tracking-tight leading-[1.1] mb-6 animate-fade-in-up animation-delay-200">
      Focus on the <span class="text-indigo-600">Process</span>.
    </h1>

    <p class="text-slate-500 text-sm md:text-base leading-relaxed max-w-md mx-auto mb-12 font-medium animate-fade-in-up animation-delay-400">
      A quiet digital studio for your fragmented thoughts.<br>
      未完成を受け入れ、思考の過程をアーカイブしよう。
    </p>

    <div class="flex flex-col sm:flex-row items-center gap-4 w-full max-w-md animate-fade-in-up animation-delay-600">
      
      <%= link_to new_user_registration_path, class: "group relative w-full py-4 rounded-xl bg-indigo-600 text-white font-['Poppins'] font-bold text-sm tracking-widest overflow-hidden transition-all hover:shadow-lg hover:shadow-indigo-500/30 hover:-translate-y-0.5 text-center" do %>
        <span class="relative z-10">GET STARTED</span>
        <div class="absolute inset-0 bg-indigo-700 translate-y-full group-hover:translate-y-0 transition-transform duration-300 ease-out"></div>
      <% end %>

      <%= link_to new_user_session_path, class: "w-full py-4 rounded-xl border border-slate-200 text-slate-600 font-['Poppins'] font-bold text-sm tracking-widest hover:border-slate-400 hover:text-slate-900 transition-all bg-white hover:bg-slate-50 text-center" do %>
        LOG IN
      <% end %>
      
    </div>

    <p class="mt-8 text-[10px] text-slate-400 font-['Space_Grotesk'] tracking-widest uppercase animate-fade-in-up animation-delay-800">
      Join the creative sanctuary
    </p>

  </div>

  <div class="relative z-10 py-6 text-center animate-fade-in animation-delay-1000">
    <p class="text-[10px] text-slate-300 font-['Inter']">
      © 2025 Atelier0. All rights reserved.
    </p>
  </div>

</div>

<style>
  @keyframes float {
    0%, 100% { transform: translate(0, 0); }
    50% { transform: translate(-30px, 30px); }
  }
  @keyframes float-reverse {
    0%, 100% { transform: translate(0, 0); }
    50% { transform: translate(30px, -30px); }
  }
  .animate-float-slow {
    animation: float 18s ease-in-out infinite;
  }
  .animate-float-reverse-slow {
    animation: float-reverse 24s ease-in-out infinite;
  }

  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px) scale(0.98);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }
  .animate-fade-in-up {
    opacity: 0;
    animation: fade-in-up 1s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
  }

  @keyframes fade-in {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  .animate-fade-in {
    opacity: 0;
    animation: fade-in 1s ease-out forwards;
  }

  .animation-delay-200 { animation-delay: 0.2s; }
  .animation-delay-400 { animation-delay: 0.4s; }
  .animation-delay-600 { animation-delay: 0.6s; }
  .animation-delay-800 { animation-delay: 0.8s; }
  .animation-delay-1000 { animation-delay: 1.0s; }
</style>