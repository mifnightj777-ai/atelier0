<div class="max-w-[2000px] mx-auto px-4 py-8">
  
  <div class="flex items-end justify-between mb-10 px-2 border-b border-slate-100 pb-6">
    <div>
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-slate-900 text-white text-[10px] font-bold uppercase tracking-widest mb-3">
        The Grid
      </div>
      <h1 class="text-4xl md:text-6xl font-['Poppins'] font-bold text-slate-900 tracking-tighter leading-tight">
        Daily Prompt Gallery
      </h1>
      <p class="text-slate-400 font-['Space_Grotesk'] text-sm md:text-base mt-2">
        A visual collection of interpretations.
      </p>
    </div>
    
    <%= link_to fragments_path, class: "group flex items-center gap-2 text-xs font-bold font-['Space_Grotesk'] text-slate-400 hover:text-slate-900 uppercase tracking-widest transition-colors mb-2" do %>
      <span>Back to Studio</span>
      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 transition-transform group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" /></svg>
    <% end %>
  </div>

  <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-1">
    
    <% if @fragments.present? && @fragments.any? %>
      <% @fragments.each do |fragment| %>
        
        <div class="group relative aspect-square bg-slate-100 overflow-hidden cursor-pointer">
          
          <% if fragment.image.attached? %>
            <%= image_tag fragment.image, class: "w-full h-full object-cover transition-transform duration-700 ease-out group-hover:scale-110" %>
          <% else %>
            <div class="w-full h-full flex flex-col items-center justify-center text-slate-300 bg-slate-50 p-4 text-center">
              <span class="font-bold text-2xl mb-1">?</span>
              <span class="text-[10px] uppercase tracking-widest">No Image</span>
            </div>
          <% end %>

          <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-all duration-300 flex flex-col justify-end p-6">
            
            <h3 class="text-white font-['Poppins'] font-bold text-lg line-clamp-1 translate-y-4 group-hover:translate-y-0 transition-transform duration-300 delay-75">
              <%= fragment.title.presence || "Untitled" %>
            </h3>
            
            <div class="flex items-center gap-2 mt-2 translate-y-4 group-hover:translate-y-0 transition-transform duration-300 delay-100">
              <div class="w-6 h-6 rounded-full bg-white/20 backdrop-blur-sm border border-white/30 flex items-center justify-center text-[10px] text-white font-bold">
                <%= fragment.user.username.first.upcase rescue "?" %>
              </div>
              <span class="text-xs text-white/90 font-['Space_Grotesk']">
                @<%= fragment.user.username rescue "unknown" %>
              </span>
            </div>
            
            <%= link_to "", fragment_path(fragment), class: "absolute inset-0 z-10" %>
          </div>

        </div>

      <% end %>
    
    <% else %>
      <div class="col-span-full py-40 text-center">
        <div class="inline-block p-6 rounded-full bg-slate-50 text-slate-300 mb-6 animate-pulse">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" /></svg>
        </div>
        <h3 class="text-slate-400 font-['Space_Grotesk'] text-xl font-bold mb-2">Gallery is Empty</h3>
        <p class="text-slate-300 text-sm">Be the first to add a fragment to this collection.</p>
      </div>
    <% end %>

  </div>
</div>