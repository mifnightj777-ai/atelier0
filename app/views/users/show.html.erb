<div class="w-full">
  
  <div class="flex flex-col items-center text-center mb-16 pt-10">
    
    <div class="w-24 h-24 rounded-full bg-gradient-to-br from-indigo-50 to-white border border-indigo-100 flex items-center justify-center text-4xl font-['Poppins'] font-bold text-indigo-600 mb-6 shadow-xl shadow-indigo-100/50">
      <%= @user.username.first.upcase %>
    </div>
    
    <div class="mb-3">
      <span class="bg-indigo-50 text-indigo-600 border border-indigo-100 text-[10px] px-3 py-1 rounded-full font-['Poppins'] font-bold tracking-widest">
        CREATOR
      </span>
    </div>
    
    <h1 class="font-['Poppins'] text-3xl font-bold text-slate-900">
      @<%= @user.username %>
    </h1>
    
    <p class="mt-2 text-sm text-slate-500 font-['Inter']">
      Atelier0 Studio Member
    </p>

    <div class="flex gap-10 mt-8 border-t border-slate-100 pt-6 px-10">
      <div class="text-center">
        <span class="block font-['Poppins'] font-bold text-xl text-slate-900"><%= @fragments.count %></span>
        <span class="text-[10px] text-slate-400 uppercase tracking-widest font-medium">Fragments</span>
      </div>
      <div class="text-center opacity-40">
        <span class="block font-['Poppins'] font-bold text-xl text-slate-900">0</span>
        <span class="text-[10px] text-slate-400 uppercase tracking-widest font-medium">Followers</span>
      </div>
    </div>

  </div>

  <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-5 gap-6 md:gap-8">
    <% @fragments.each do |fragment| %>
      <div class="group relative transition-all duration-300 ease-out hover:-translate-y-2 hover:rotate-2">
        <div class="bg-white p-2 rounded-[20px] shadow-[0_8px_30px_rgb(0,0,0,0.08)] border border-slate-100 group-hover:shadow-[0_20px_40px_rgb(99,102,241,0.2)] transition-shadow">

          <div class="w-full aspect-[4/5] overflow-hidden rounded-[12px] bg-slate-100 relative mb-3">
            <% if fragment.private_view? %>
              <div class="absolute top-2 right-2 z-30 bg-black/60 text-white p-1.5 rounded-full backdrop-blur-sm border border-white/20 shadow-md">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-3 h-3">
                  <path fill-rule="evenodd" d="M10 1a4.5 4.5 0 00-4.5 4.5V9H5a2 2 0 00-2 2v6a2 2 0 002 2h10a2 2 0 002-2v-6a2 2 0 00-2-2h-.5V5.5A4.5 4.5 0 0010 1zm3 8V5.5a3 3 0 10-6 0V9h6z" clip-rule="evenodd" />
                </svg>
              </div>
            <% end %>

            <% if fragment.image.attached? %>
              <%= image_tag fragment.image, class: "w-full h-full object-cover" %>
            <% else %>
              <div class="w-full h-full flex flex-col items-center justify-center bg-indigo-50 text-indigo-300">
                <span class="font-['Poppins'] font-bold text-xl">?</span>
              </div>
            <% end %>
          </div>

          <div class="px-1 pb-2">
             <% if fragment.description.present? %>
              <h3 class="font-['Poppins'] font-bold text-slate-800 text-sm line-clamp-1 mb-1">
                <%= fragment.description %>
              </h3>
             <% else %>
              <h3 class="font-['Poppins'] font-bold text-slate-300 text-sm mb-1">Untitled</h3>
             <% end %>
             
             <div class="flex justify-between items-center text-[10px] font-['Inter'] text-slate-500">
               <span class="bg-slate-100 text-slate-500 px-2 py-0.5 rounded-full font-medium truncate max-w-[50%]">
                 @<%= fragment.user.username %>
               </span>
               <span><%= fragment.created_at.strftime("%Y.%m.%d") %></span>
             </div>
          </div>
        </div>
        <%= link_to "", fragment, class: "absolute inset-0 z-10" %>
      </div>
    <% end %>
  </div>

  <% if @fragments.empty? %>
    <div class="mt-20 flex flex-col items-center justify-center text-center opacity-60">
      <p class="font-['Poppins'] text-slate-500 font-medium">No fragments yet.</p>
    </div>
  <% end %>

</div>